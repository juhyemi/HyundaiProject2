<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.chysk5.mapper.ResellMapper">
  
  	<select id="getMyResellProduct" resultType="com.chysk5.domain.ResellProductDTO">
  		SELECT pro_name, pro_opt_size, pro_price, pro_opt_color
		FROM product p
		INNER JOIN product_option po
		ON p.pro_id = po.product_pro_id
		WHERE pro_opt_id = #{pro_opt_id}
  	</select>
  	
  	<insert id="register">
  		INSERT INTO r_product (re_id, re_price, re_regdate, re_update, re_available, member_mem_id, product_option_pro_opt_id)
		VALUES (#{re_id}, #{re_price}, sysdate, sysdate, #{re_available}, #{member_mem_id}, #{product_option_pro_opt_id})
  	</insert>
  	
  </mapper>
  